-- replication を完全にリセットするSQLスクリプト
-- 注意: これを実行するとレプリケーションの設定と履歴が完全に消去されます。

-- 0. レプリカの停止
STOP REPLICA;

-- 1. レプリカとしての設定（マスター接続情報など）をリセット
RESET REPLICA ALL;

-- 2. マスターとしての履歴（自分のGTID実行履歴）をリセット
RESET MASTER;
